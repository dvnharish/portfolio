# Absolute origin of the deployed site. Used for canonical URL, OG tags,
# sitemap and JSON-LD. No trailing slash.
# Production default (baked into lib/site.ts) is https://harishduddupudi.com —
# only set this to override, e.g. on a preview deploy.
NEXT_PUBLIC_SITE_URL=https://harishduddupudi.com
